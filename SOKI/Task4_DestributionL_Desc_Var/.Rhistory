testFn0 <- function(x) {
prod(cos(x))
}
adaptIntegrate(testFn0, rep(0,2), rep(1,2), tol=1e-4)
install.packages("cubature")
install.packages("possibly")
testFn0 <- function(x) {
prod(cos(x))
}
adaptIntegrate(testFn0, rep(0,2), rep(1,2), tol=1e-4)
testFn0 <- function(x) {
prod(cos(x))
}
adaptIntegrate(testFn0, rep(0,2), rep(1,2), tol=1e-4)
install.packages("cubature")
testFn0 <- function(x) {
prod(cos(x))
}
adaptIntegrate(testFn0, rep(0,2), rep(1,2), tol=1e-4)
install.packages('cubature')
require(cubature)
testFn0 <- function(x) {
prod(cos(x))
}
adaptIntegrate(testFn0, rep(0,2), rep(1,2), tol=1e-4)
M_2_SQRTPI <- 2/sqrt(pi)
install.packages('cubatue')
require(cubature)
